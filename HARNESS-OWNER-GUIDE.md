# 하네스 담당자 안내서 — "내 하네스만 키우고 검증하기"

부서에서 특정 하네스 하나를 맡았을 때 읽는 문서입니다.
(예: 품질·메타 담당 = `data-standard`, 보안 담당 = `secure-sdlc` 등)

> 핵심: **베이스(중앙 몸통·규격·core)는 안 건드립니다.** 내 하네스 폴더만 키우면 됩니다.
> 다른 팀 진도와 상관없이, 윗단의 *예시 산출물*만 있으면 혼자 개발·검증할 수 있습니다.

---

## 1. 내 하네스 폴더만 보면 됨

```
<내하네스>-kit/
├── manifest.yaml       자기소개 (중앙 몸통이 이것만 보고 부름)
├── policy.yaml         🟡 내가 정하는 규칙
├── gates/*.sh          🟢 내가 만드는 검사
├── *.template.yaml     빈 양식
├── *.example.yaml      좋은 예시
├── tests/              ★ 자가진단 (좋은/나쁜 예시)
│   ├── cases.yaml
│   └── fixtures/
└── MANUAL.md 등 문서
```

---

## 2. 개발 사이클 (3단계 반복)

### ① 규칙을 바꾼다
`policy.yaml`(내 규칙)이나 `gates/*.sh`(내 검사)를 고친다.

### ② 자가진단으로 확인한다 ★ 가장 중요
새 규칙을 어기는 **나쁜 예시**를 `tests/fixtures/` 에 넣고, `tests/cases.yaml` 에 케이스를 추가한 뒤:

```bash
spine/harness-test.py <내하네스id>
```

→ "좋은 입력은 통과(✅), 나쁜 입력은 차단(✅ 정상 차단)" 이 한 번에 확인됩니다.
**이게 게이트가 진짜로 거르는지 검증하는 작업**입니다. (담당자의 핵심 일)

### ③ 파이프라인에 잘 끼는지 통합 확인한다
```bash
spine/spine.py run --only=<내하네스id> --demo
```

→ 내 하네스 + 의존하는 윗단만 골라서 전체 흐름에 끼워 돌려봅니다.

### ④ 베이스에 올리기 전 규격 검수를 받는다
```bash
spine/verify-harness.py <내하네스id>
```

→ 매니페스트 형식·비밀값·빈칸·자가진단 통과 등 **규격 적합성**을 자동 검수합니다.
여기서 ✅ 가 다 떠야 베이스에 올릴 수 있습니다.
(단, 검증기는 '형식·안전'만 봅니다. 규칙이 업무적으로 옳은지는 동료 리뷰가 봅니다.)

---

## 3. 실제 예시 (data-standard 로 한 바퀴)

```bash
# 자가진단: 좋은 1개 통과 + 나쁜 4개 차단
$ spine/harness-test.py data-standard
  ✅  정상 데이터계약은 이름검사 통과
  ✅(정상 차단)  대문자 테이블/컬럼명은 막혀야
  ✅(정상 차단)  허용목록에 없는 등급은 막혀야
  ✅(정상 차단)  흐름(lineage) 비면 막혀야
  ✅(정상 차단)  빈칸(미정) 남으면 막혀야
  🎉 좋은 입력은 통과, 나쁜 입력은 막음

# 통합: 내 것 + 윗단(policy-profile)만
$ spine/spine.py run --only=data-standard --demo
  ▶ 대상 ['data-standard', 'policy-profile']
  ... ✅ check-naming / ✅ check-metadata-complete
```

---

## 4. 새 검사 규칙을 추가하는 방법 (테스트 주도)

1. **나쁜 예시 먼저** — `tests/fixtures/bad-새규칙.yaml` 에 그 규칙을 어기는 데이터를 만든다.
2. `tests/cases.yaml` 에 `expect: fail` 케이스를 추가한다.
3. `harness-test.py` 를 돌린다 → 아직 게이트가 안 막으면 ❌ 가 뜬다.
4. `gates/` 또는 `policy.yaml` 에 규칙을 추가해 ❌ 를 ✅(정상 차단) 으로 바꾼다.
5. 좋은 예시(`good.yaml`)는 여전히 통과하는지 같이 확인된다.

→ "나쁜 예시 → 게이트 → 통과" 순서가 곧 테스트 주도 개발입니다.

---

## 5. 의존 윗단이 아직 없으면? (다른 팀이 안 만들었을 때)

내 하네스는 윗단의 **결과물 예시**에만 의존합니다(코드 아님).
그 예시 파일(fixture)만 있으면 혼자 개발할 수 있습니다.
예: data-standard 는 policy-profile 의 `profile.example.json` 만 있으면 됨.

윗단이 아직 없으면, 내가 기대하는 입력 형태의 **가짜 예시 파일**을 직접 만들어 두고 개발하면 됩니다.
나중에 진짜가 와도 형식(스키마)만 맞으면 그대로 동작합니다.

---

## 6. 절대 규칙 (어기면 안 됨)

- `manifest.yaml` 의 표준 형식을 지킨다 (중앙 몸통이 이것만 보고 부름).
- 실제 주소·접속코드·개인정보를 어떤 파일에도 넣지 않는다.
- 게이트는 통과(exit 0)/차단(exit 0 아님) 종료코드를 정확히 낸다.
- 다른 하네스 폴더 내부를 직접 부르지 않는다 (의존은 manifest 의 requires + 산출물로만).
