# _archive — 보관용 (평소 무시)

더 이상 활성으로 쓰지 않는 옛 테스트 입력 모음입니다.
어떤 스크립트도 참조하지 않으며, `run-all.sh`(비재귀)에도 잡히지 않습니다.

- `mock1~5-*.yaml` — 초기 모의 입력(예금/카드/위반/인스턴스)
- `sets/set1~5/` — 변환 완료(INPUT) 형태의 DB설계·인스턴스 5세트

> 활성 시연 데이터는 `../freeform/` 를, 게이트 기본 입력은 `../*.yaml` 를 쓰세요.
