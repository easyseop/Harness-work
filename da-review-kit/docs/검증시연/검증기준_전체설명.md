# DA검토 하네스 — 검증기준 전체 설명 (클로드 디자인 참고용)

> 게이트 **3개**, 검증 항목 **29개**. 크게 **DB설계도(23) / 인스턴스코드(11)** 두 갈래.
> 검사는 결정적(LLM 미사용) — 같은 입력=같은 결과. 영문변수명 = **camelCase(첫글자 소문자)**.
>
> **출처 범례** — ① 데이터모델 설계지침 · ② 상시검토 가이드 · ③ 데이터표준 설계지침 · ④ 명명규칙 PPTX · ⑤ 인스턴스코드 정의서
> 🔴 = 우리가 만든 것(mock·조직결정·설계) · 🟡 = 개념은 문서·값은 확인필요

---

# A. DB설계도 검토 (게이트: check-physical + check-attribute) — 23개

## A-1. 테이블명 (check-physical · 4)
| # | 검증기준 | 통과조건 | 오류예 → 사유 | 출처 |
|---|---|---|---|---|
| P1 | 명명규칙 패턴 | `T`+시스템(`H/S`)+앱(3)+소그룹(2)+일련(2)=9자 만족 | `TXLONBA01` → 「테이블명이 명명규칙에 안 맞음」 | ④, ② |
| P2 | 앱코드 등록 | 앱코드(3)가 표준 등록목록(DPS/LON/CUS…)에 있음 | `TSZZZAA01` → 「어플리케이션코드 'ZZZ' 미등록」 | ② / 🔴코드값 mock |
| P3 | 소그룹코드 등록 | 소그룹코드(2)가 등록목록(AA/AB…)에 있음 | `TSDPSZZ01` → 「업무소그룹코드 'ZZ' 미등록」 | ② / 🔴코드값 mock |
| P4 | 시스템구분 | 2번째 글자가 `H`(호스트)/`S`(서버) | (패턴에서 1차 차단) | ④ |

## A-2. 컬럼 이름조합·인포타입 (check-physical · 10)
대전제: 컬럼명 = `[수식어]+[도메인 끝말]`, 인포타입 = `도메인+길이`.

| # | 검증기준 | 통과조건 | 오류예 → 사유 | 출처 |
|---|---|---|---|---|
| P5 | 도메인으로 끝남 | 한글명이 표준 도메인(성별·번호·금액…)으로 끝남 | `고객연식` → 「'고객연식' 가 도메인으로 끝나지 않음」 | ④ |
| P6 | 수식어 표준단어 | 끝말 앞 수식어가 전부 표준단어 | `임시성별` → 「수식어 '임시' 에 비표준단어 포함」 | ③, ② / 🔴영문약어 mock |
| P7 | 한글↔영문 일치(camelCase) | 영문명 = 한글 표준약어 조합(첫글자 소문자) `고객성별`→`custSex` | `custGender` → 「영문명 'custGender' 불일치 (기대 'custSex')」 | ③ 개념 / 🔴camelCase 조직결정 |
| P8 | 인포타입 도메인 일치 | 인포타입 도메인 = 컬럼 끝말 도메인 | `고객성별`+`년월일8` → 「인포타입 도메인 '년월일' 이 끝말 '성별' 과 불일치」 | ③, ④ |
| P9 | 인포타입 길이 허용 | 길이가 도메인 허용목록 안(복수 허용: 금액 15·18.3) | `성별10` → 「인포타입 길이 '10' 비허용 (허용 ['1'])」 | ③,④ (금액 ✅) / 🟡그외 값 |
| P10 | 한글명 길이 | Server 15자 / Host 10자 이내 | 16자 → 「한글명 16자 > 최대 15자」 | ③ 개념 / 🟡수치 |
| P11 | 한글명 필수 | 한글명 기입됨 | 빈칸 → 「컬럼 한글명 누락」(missing) | ③ |
| P12 | 영문명 필수 | 영문명 기입됨 | 빈칸 → 「영문명 누락」(missing) | ③ |
| P13 | 인포타입 필수 | 인포타입 기입됨 | 빈칸 → 「인포타입 미기재」(missing) | ③, ④ |
| P14 | 끝자리 숫자(옵션) | 스위치 ON일 때만 끝숫자 금지 | (현재 정책=허용 OFF) | ② 금지 ↔ 🔴조직결정 허용 |

## A-3. 구조·관계 (check-physical · 5)
| # | 검증기준 | 통과조건 | 오류예 → 사유 | 출처 |
|---|---|---|---|---|
| P15 | PK 필수 | pk 컬럼 1개 이상 | 없음 → 「PK(식별자) 미설정」(missing) | ① §6·§14 |
| P16 | M:N 직접관계 금지 | 관계가 1:1 / 1:M | `M:N` → 「M:N 직접관계 — 연결엔티티로 분해 필요」 | ① §4 |
| P17 | 감사컬럼 권장(경고) | `sysLstPrcDtm`,`sysLstUsrNo` 존재 | 없음 → ⚠️경고(반송 아님) | ① 개념 / 🔴컬럼명 표기 |
| P18 | 표준화적용여부=N | — | `std:false` 컬럼 검사 **건너뜀** | ③ 표준화적용여부 / 🔴처리 |
| P19 | 비표준 테이블 | — | `standard:false` 테이블 **검토 제외** | 🔴 판정 메커니즘(우리 설계) |

## A-4. 속성명 (check-attribute · 4)
컬럼명과 별개로 "속성명"(컬럼의 도메인 역할)을 검사. 속성명 없으면 한글컬럼명을 속성명으로 간주.

| # | 검증기준 | 통과조건 | 오류예 → 사유 | 출처 |
|---|---|---|---|---|
| A1 | 속성명 도메인 종료 | 속성명이 도메인으로 끝남 | `머시기머시기` → 「속성명 '…' 가 도메인으로 끝나지 않음」 | ① §5, ③ |
| A2 | 속성명 수식어 표준 | 속성명 수식어가 표준단어 | `머시기명` → 「속성명 '머시기명' 수식어 '머시기' 비표준」 | ③, ② |
| A3 | 컬럼↔속성 동일 도메인 | 컬럼 끝말 도메인 = 속성 끝말 도메인(동종속성 동일도메인) | 컬럼`고객성별`/속성`고객등급` → 「도메인('성별')이 속성('등급')과 불일치」 | ① §5, ② |
| A4 | 컬럼 수식어 ⊆ 속성 수식어 | 컬럼 수식어가 속성명에 모두 포함 | 컬럼`직원상태구분코드`/속성`상태구분코드` → 「수식어 ['직원'] 가 속성에 없음」 | ① 개념 / 🔴구체규칙 |

---

# B. 인스턴스코드 검토 (게이트: check-instance-code) — 11개
2시트: 시트1=업무인스턴스명정의서, 시트2=업무인스턴스코드정의서.

## B-1. 시트1 명명규칙 (6)
| # | 검증기준 | 통과조건 | 오류예 → 사유 | 출처 |
|---|---|---|---|---|
| I1 | 인스턴스명 필수 | 이름 기입됨 | 빈칸 → 「인스턴스명 누락」 | ⑤ |
| I2 | 공백/특수문자 금지 | 한글/영문/숫자만 | `계좌 상태구분코드` → 「공백/특수문자 사용 불가」 | ⑤ 개념 / 🟡 |
| I3 | 단독사용금지 12단어 | 자산·부채·금액·여부 등을 단독 이름으로 안 씀 | `여부` → 「단독사용금지 단어」 | ⑤ / ③ (문서근거) |
| I4 | 중복 금지 | 인스턴스명 유일 | 동일명 2회 → 「인스턴스명 중복」 | ⑤ 개념 / 🟡 |
| I5 | 끝말 규칙 | `~구분코드/~코드/~여부/~유무` 로 끝남 | `계좌상태` → 「끝말 규칙 위반」 | ⑤ (목록 🟡확인) |
| I6 | 수식어 표준단어 | 끝말 앞 수식어가 표준단어 | `임시구분코드` → 「수식어 '임시' 비표준」 | ③, ② |

## B-2. 시트2 코드값 규칙 (5)
| # | 검증기준 | 통과조건 | 오류예 → 사유 | 출처 |
|---|---|---|---|---|
| I7 | 시트 정합 | 시트2 인스턴스명이 시트1에 존재 | `유령구분코드` → 「시트2 인스턴스명이 시트1에 없음」 | ⑤ |
| I8 | 여부/유무 = 0/1 | `~여부/~유무` 코드값이 0,1만 | `0,1,2` → 「~여부 는 코드값 0/1 만 허용」 | 🟡 통념(여부 0/1) |
| I9 | 코드 길이 | 코드 자릿수 = code_length | len1인데 `00` → 「코드 '00' 길이가 code_length(1)와 다름」 | ⑤ (코드길이 항목) |
| I10 | 0=해당무(경고) | 코드 0의 내용이 '해당무' | `0=입금` → ⚠️경고 | 🟡 예약의미 통념 |
| I11 | 9=기타(경고) | 코드 9의 내용이 '기타' | `9=출금` → ⚠️경고 | 🟡 예약의미 통념 |

---

## 유형·합계
- 🔴차단(반송): P1~P16 · A1~A4 · I1~I9 — 사유 + **권고(고치는 법)** 같이 출력
- 🟠경고(통과+주의): P17 · I10 · I11
- ⚪건너뜀(검토 제외): P18 · P19
- **합계: 게이트 3개 / 검증 29개 (DB설계 23 + 인스턴스 11)**

## 클로드 디자인 메모
- 위 표를 **A.DB설계도 / B.인스턴스코드** 2개 대섹션으로 슬라이드 구성. 표 그대로(문구 변경 금지), 디자인만.
- **출처 컬럼**을 색으로 구분: 문서번호(①~⑤)=네이비 · 🔴=빨강/회색 · 🟡=주황. "어디까지 문서근거이고 어디부터 보강 필요한지" 한눈에.
- 유형(🔴/🟠/⚪)도 색 뱃지. 영문명은 monospace(camelCase 강조).
